# Decisions (ADR log)

Rule: if a choice is small, decide, write it here, move on. Newest decisions are appended at the end.
Status: ✅ decided · ⚠️ needs owner input (see "Open owner actions" at the bottom).

| # | Decision | Why |
|---|---|---|
| D-001 | **NAS is the source of truth.** PostgreSQL, Redis, MinIO, API, WebSocket, bot worker, web, logs, backups all run on our NAS in Docker. Phones are clients only. | Business requirement. |
| D-002 | **External services only as transport:** Telegram Bot API (registration, one-time login codes, notifications) and Yandex MapKit (map tiles on the phone). No business database/storage in any cloud. Push payloads / Telegram messages never contain money, collateral details or full addresses. | Telegram/Yandex are unavoidable for the chosen UX; keep data out of them. |
| D-003 | **Single organisation (no multi-tenancy).** No `organizationId` columns. | Home business; halves the code and the test surface. Adding tenants later = one migration + query scope (accepted cost). |
| D-004 | ~~Two roles only: `ADMIN`, `WORKER`.~~ **Superseded by D-028** (owner, 2026-09-22): four roles, `SUPER_ADMIN` and `MANAGER` added. | Simple first — outgrown once the owner needed managers and a system owner distinct from day-to-day admins. |
| D-005 | **Telegram Bot = interface, NestJS = business logic.** The bot runs in a separate `worker` container (same codebase/image, different entrypoint), uses **long polling** (no inbound webhook → works behind CGNAT), keeps conversation state in PostgreSQL (`RegistrationDraft`), and only translates Telegram updates ⇄ `RegistrationService` calls. | Bot restarts never lose a half-filled registration; logic is unit-testable without Telegram. |
| D-006 | **Outgoing Telegram messages go through a DB outbox** (`Notification`, channel `TELEGRAM`). The API never talks to Telegram; the bot process delivers the outbox. | API keeps no internet egress; delivery is retried and auditable. |
| D-007 | **ADMIN signs in with phone + password (argon2id). WORKER signs in with a 6-digit one-time code sent by the bot to her Telegram** (5 min, single use, max 5 attempts, stored hashed). Refresh-token rotation with reuse detection, per-device sessions, login lockout. | Workers have no passwords to forget; no SMS provider needed; we already know her Telegram chat. |
| D-008 | **Realtime = Socket.IO gateway in the API + Redis pub/sub bus.** Events are published **only after the DB transaction commits**. The bus is Redis in production (so the bot process can notify ADMIN sockets) and an in-process emitter when `REDIS_URL` is unset (tests). Clients treat events as "something changed" and refetch; the API stays the source of truth. | Required by the brief (§12–13). |
| D-009 | **Money = `BigInt` whole UZS**; metres = `Decimal(10,2)` computed in integer centimetres. Money travels as decimal strings in JSON. | No floats anywhere near money. |
| D-010 | **Immutability is enforced by PostgreSQL triggers** on `audit_logs`, `worker_ledger_transactions`, `collateral_history`, `stock_movements`, `work_assignment_status_history`, `pay_rate_changes` (no UPDATE/DELETE/TRUNCATE). Fixes are compensating rows (`CORRECTION`). | An application bug cannot rewrite history. |
| D-011 | **Stock = movements** (`StockMovement`) + materialised `StockBalance` (CHECK ≥ 0). **Materials held by a worker are derived from movements** (view `worker_material_holdings`), not a second table. | One source of truth, no double bookkeeping. |
| D-012 | **QR carries only an opaque code** (`YQ1.` + 12 base32 chars) stored in `QrEntity`; scanning calls `GET /v1/qr/:code` (ADMIN only). No personal data in the code. | Brief §14. |
| D-013 | **Maps:** Yandex MapKit (Flutter `yandex_maps_mapkit_lite`, see D-026) shows workers from GPS received via Telegram; "Call" and "Route" use `tel:` / Yandex Maps deep links (no API key needed for those). MapKit key is supplied with `--dart-define=YANDEX_MAPKIT_KEY`. ⚠️ | Brief §8. |
| D-014 | **Files:** MinIO (S3 API) on the NAS; the API proxies uploads (validates real image bytes, strips EXIF, thumbnails) and serves short-lived HMAC-signed URLs. Photos sent to the bot are downloaded by the worker process and stored in MinIO at once. PostgreSQL keeps metadata only. | Controlled access, nothing public. |
| D-015 | **MinIO upstream is archived (Apr 2026)**; official images only on `quay.io`. We pin `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z.hotfix.7aa24e772`, keep MinIO on the internal Docker network (never public), and only use the S3 API (`StoragePort`) so it can be swapped for Garage/SeaweedFS by config + `mc mirror`. ⚠️ plan a migration in the "Improvements" phase. | Honest risk record. |
| D-016 | **Secrets never in Git.** `.env` is generated on the NAS by `infra/scripts/nas-init.sh` with random secrets; `.env.example` is the template. | Security. |
| D-017 | **Reverse proxy = Caddy.** Exactly one port is published (see D-025 for why it is plain HTTP, not 80/443). `/health/ready` and Swagger are blocked at the proxy. PostgreSQL/Redis/MinIO live on an `internal` Docker network. | Simplest secure setup on a NAS. |
| D-018 | **Toolchain pinned for stability:** Node 24, pnpm 12 (`allowBuilds` whitelist), NestJS **11** (12 is ESM-only), Prisma **6** (npm `latest` is an RC), TypeScript **5.9**, PostgreSQL **17**, Redis 7.4, Next.js 16, Flutter 3.47, grammY. | Avoid unproven majors in a money system. |
| D-019 | **Backups stay under our control:** nightly `pg_dump` (+ checksum), continuous WAL archive + weekly base backup (PITR), MinIO mirror without delete propagation, config snapshot, automatic restore-verification. NAS snapshots are configured on the NAS itself. No cloud copy unless the owner decides later (encrypted, secondary only). | Brief §57. |
| D-020 | **UI is replaceable:** Flutter is split into `core/` (network, db, realtime, theme tokens), `features/<x>/{data,application,presentation}`; screens contain no business rules and read only providers. All colours/typography come from one `AppTheme`. Backend DTOs are stable and UI-agnostic. | The owner will iterate on design after seeing screens (§61). |
| D-021 | **Registration → approval:** the bot creates `WorkerProfile(PENDING_APPROVAL)` and a **declared** collateral (`PENDING`). ADMIN approves (worker becomes `ACTIVE`, receives a personal QR, gets a Telegram message) and confirms the collateral as physically received (`HELD`). Rejection keeps the record (audit) with a reason. | A declaration is not custody. |
| D-022 | **Offline (ADMIN app):** reads come from a local Drift cache; only non-critical changes may be queued. Money, collateral, stock, issuing, acceptance and payouts require server confirmation. | Brief §56. |
| D-023 | **Build only what the milestone needs.** The Prisma schema contains the whole §58 model from M0 (schema-first); services/UI are added per milestone. No mocks in critical flows. | Simple first, but honest. |
| D-024 | **Workers are paid per 9 m kit, never per metre** (owner, 2026-09-21). The price is UZS per kit (D-027: ONE global price, 30 000 at the start). `earning = round_half_up(ratePerKit × acceptedCm / 900)`: 9/18/27 m pay exactly 1/2/3 × rate; a partly accepted kit is paid pro rata — ✅ confirmed by the owner 2026-09-21 (alternative, not chosen: whole kits only = flooring in `earningFor`). Migrations `20260921180000_rate_per_kit`, `20260921190000_global_pay_rate`. | Matches how the business prices work; the pro-rata default never pays a worker nothing for 8 good metres. |
| D-025 | **Our NAS is shared and already busy** (UGREEN DXP4800 Plus, x86_64, Debian 12, Docker 29 / Compose 5, 62 GB RAM, /volume1 9 TB; ≈30 containers of other projects; host ports 80/443 belong to the NAS, also 3000/3001/3005/3100/4100 etc.). Therefore: (1) the proxy listens on plain HTTP and publishes **one** port, `HTTP_PORT` (8088), bound to `127.0.0.1` by default; (2) internet HTTPS = **optional Cloudflare Tunnel** (compose profile `tunnel`, outbound only, works behind CGNAT — the NAS already runs such tunnels; Cloudflare then sees HTTP traffic, which stays the owner's explicit choice per D-002); (3) fixed Docker subnets 172.30.61–63.0/24; (4) no `container_name`, everything under `/volume1/docker/yusmus`; (5) one public origin (`PUBLIC_URL`) with path routing replaces the two domains. Replaces the Let's Encrypt/80/443 design. | Ports 80/443 cannot be used; avoids touching the other projects. |
| D-026 | **Map package = Yandex's official `yandex_maps_mapkit_lite`** (verified publisher maps.yandex.ru, v4.45.0), not the community `yandex_mapkit`. Lite covers map, markers, user location; route building (Full SDK) is not needed because **Route** is a deep link. Licence: free up to 25 000 monthly active users. | Official, maintained, free at our scale. |
| D-027 | **ONE global price for a 9 m kit: 30 000 UZS (owner, 2026-09-21), changeable by ADMIN at any moment, and the change applies to everybody.** Table `pay_rate_changes` (append-only: who, when, previous → new, note; current = highest `seq`; seeded with 30 000 by the migration); `GET /v1/settings/pay-rate` (ADMIN + WORKER), `PUT` (ADMIN only, 1 … 10 000 000, concurrent changes serialised by an advisory lock, same value = no-op), `GET …/history` (ADMIN). Every change is audited and broadcast as `pay_rate.changed` to ALL admin and worker screens. **Work that is not yet accepted is paid at the current price** (no rate is copied into assignments); on acceptance the amount goes into the immutable ledger and `settledRatePerKit` records the price used, so **accepted / paid work is never recalculated**. Per-variant rates were removed. Workers are not told about a change in Telegram (D-002: no money in Telegram); their app shows it live. | The owner wants one price they can change instantly for everyone; history and the ledger keep money honest. |
| D-028 | **Four roles, ranked: `SUPER_ADMIN` (4) > `ADMIN` (3) > `MANAGER` (2) > `WORKER` (1)** (owner, 2026-09-22, master prompt). A user may manage (create/deactivate/reassign role/permissions) only a **strictly lower rank**; SUPER_ADMIN manages everybody; nobody manages themselves, ever, even SUPER_ADMIN. **Fine-grained permissions** on top of the role (`packages/shared/src/permissions.ts`): SUPER_ADMIN always has all of them; ADMIN and MANAGER have documented defaults (`ROLE_DEFAULT_PERMISSIONS`) and can be granted/revoked individual ones (`user_permissions`, never `ROLE_ASSIGN`/`PERMISSION_MANAGE` — SUPER_ADMIN-only forever). WORKER has none (fixed, her own routes use `@Roles('WORKER')` directly). Every worker-scoped read (`WORKER`, `COLLATERAL`, `ASSIGNMENT`, `FINANCE`, `LOCATION`, `MAP` categories) is filtered SERVER-SIDE by `scopeFor()`: `_ALL` permission = everyone, `_ASSIGNED` = only `WorkerProfile.assignedManagerId = viewer.id` (never a client-supplied id), `none` = 403. A worker outside scope answers 404 (existence not revealed), inside-scope-but-missing answers 404 too. Realtime rooms are computed by the exact same `scopeFor` (`roomsForUser`/`roomsForEvent`, `packages/shared/src/events.ts`) so REST and WebSocket can never disagree. See [RBAC.md](RBAC.md). | The master prompt's core requirement: SUPER_ADMIN has guaranteed full control, a MANAGER must never see another manager's workers, "permission разрешает" for ADMIN on money/settings. |
| D-029 | **Catalog "Наши работы"** (owner, 2026-09-22): `ProductModel` gained `status` (DRAFT/PUBLISHED/HIDDEN), `availability`, `isNew`, `sortOrder`; new `ProductMedia` (ordered photos/videos in MinIO, one `isMain`) and `CompanyContactSettings` (single row: phone + Telegram, never hard-coded in the apps). WORKER's default screen; **no price field exists anywhere in these tables** — nothing here ever reads `pay_rate_changes`. `CATALOG_VIEW`/`CATALOG_MANAGE` gate the staff routes (`/admin/catalog/...`); `@Roles('WORKER')` gates the public ones (`/catalog`, published only). Deleting a used item (real assignment/sale) is blocked — hide it instead. | Brief §10-17: informational catalog, "Позвонить"/Telegram, never a checkout. |
| D-030 | **Live working location, every role** (owner, 2026-09-22): `UserLiveLocation` keeps only the LATEST position per user (no unbounded history — a location "on file", not a trail) via `POST /v1/location` (self-report only; the server ignores any id the client sends). The Flutter app never pretends to switch on a system setting: a `Geo` abstraction (`geolocator` + `permission_handler`) drives an ordered check — Location Services → foreground permission → background permission — and a blocking `LocationGate` screen names exactly what is missing and only calls the matching system action. Movement-triggered updates (25 m) plus a 5-minute heartbeat (`LocationTracker`) are battery-conscious by construction. `GET /v1/locations` is scoped exactly like worker data (`LIVE_LOCATION_VIEW_ALL`/`_ASSIGNED`) and flags a position `stale` past 15 minutes rather than presenting it as current. A worker's live report also refreshes her classic `WorkerProfile.latitude/longitude` (what the future Yandex map, M4, will read). | Brief §19-26: background location for all four roles, explicit permission flow, staleness honesty. |
| D-031 | **Presence (online/offline) is separate from GPS** (owner, 2026-09-22): `PresenceService` tracks LIVE Socket.IO connections per user in memory (a user is online while ≥ 1 socket is open) and writes `User.lastSeenAt` on connect/disconnect/heartbeat (`presence:ping`) as the fallback that survives a restart. `user.presence_changed` is broadcast to `USER_VIEW_ALL` and to the user's own manager. | Brief §22: "Последняя геолокация: X минут назад", not a fake "online" derived from a stale GPS point. |
| D-032 | **SUPER_ADMIN migration on an existing database is explicit, never automatic.** `bootstrap.js` (fresh database) now creates `SUPER_ADMIN` by default and refuses a second one; `promote-owner.js` (existing database) promotes one already-existing ADMIN/MANAGER, requires the phone typed twice identically, never creates an account, and revokes that user's sessions. | Master prompt §9: "не присваивай SUPER_ADMIN случайному пользователю"; explicit > implicit for an irreversible-feeling change. |
| D-033 | **The web app becomes a real control panel**, not a status page: a client-side React app (Next.js App Router, `'use client'` throughout) behind a phone+password login, calling the SAME public API the Flutter app uses. Tokens in `localStorage` (a staff tool behind a login, same trust model as the mobile app's secure storage), silent refresh on 401. In production it shares one origin with the API (Caddy path-routing, D-025) so CORS never applies there; local development needs `CORS_ORIGINS` set to the panel's dev origin. | Brief §29-33: a full SUPER_ADMIN web control panel, not a dashboard-less notebook replacement. |
| D-034 | **Materials/Stock become a real API on top of the M0 schema, not a new design.** `Material`/`StockMovement`/`StockBalance` already existed (unused). M2 adds `INVENTORY_VIEW`/`INVENTORY_MANAGE`-gated routes; every movement runs through ONE method, `StockService.recordMovement`, which row-locks `stock_balances` first (`lockMaterialBalance`, keyed by `materialId` — the table's actual PK, not `id`) so two concurrent movements on the same material can never both pass the ≥0 check. Categories are the owner's fixed 5 (TAPE/BEAD/THREAD/ACCESSORY/OTHER), seeded as `MaterialCategory` rows with a stable `code`, not a hard Postgres enum — the RU label can be renamed later without a migration; not user-creatable this round. | Owner §5-7: a real ledger, not a mutable counter; "не переписывай рабочие части" honoured by reusing the M0 tables as-is. |
| D-035 | **The 9 m kit template is ONE recipe; 18/27 m are the same recipe assembled ×2/×3, never a second template** (owner §8-9, explicit). `POST /admin/kits/:id/assemble { count }` multiplies every `MaterialKitTemplateItem.requiredQuantity` by `count` and consumes it from the warehouse as one atomic group of `ISSUE_TO_KIT` movements (shared `groupId`), refusing the WHOLE batch (409) if any single ingredient is short — never a partial consumption. `QrType` gained `KIT` (additive; the new enum value is never referenced in the same migration transaction it was added in, same pattern as D-028's role values) — `QrEntity.kitTemplateId/kitCount/stockGroupId` describe the assembled batch. Kept the existing `ISSUE_TO_KIT` movement-type name from M0 rather than renaming it to the owner's "KIT_ASSEMBLY" wording (same meaning; avoids an unnecessary rename on an append-only-triggered table). | Owner §8-11: one recipe, a real physical batch, one QR per batch, ready for the future WorkAssignment QR to reuse. |
| D-036 | **QR resolve reuses the exact scope check of the thing it resolves to — never a parallel authorization path.** `GET /qr/:code` for a WORKER code calls `WorkersService.get()` verbatim, so a MANAGER scanning a worker outside her assigned group gets the identical 404 that route already gave; a KIT code needs `INVENTORY_VIEW`; an unknown/malformed/revoked code, or the not-yet-issued `ASSIGNMENT` type, is a plain 404, never a guess. Fixed a latent bug found while wiring this up: `/audit` was `@Roles('ADMIN')`, silently excluding SUPER_ADMIN — changed to `@Perm('AUDIT_VIEW')` (added to `ADMIN_DEFAULTS`; never grantable to MANAGER, audit is store-wide). | Owner §10-13: staff scan a worker/kit QR; SUPER_ADMIN always has every permission, including ones a screen forgot to check. |
| D-037 | **The Yandex map moves from M4 into M2** (owner, 2026-09-22: "не откладывать до M4"), on top of the live-location data the earlier round already built — no new backend design. `GET /locations` gained `phone` and live `online` presence so a marker needs no second request, and a shared `freshness` tier (`LIVE`/`RECENT`/`STALE`, thresholds in `packages/shared` so the API/Flutter/web can never disagree; the old `stale` boolean is kept, now `= freshness === 'STALE'`). Flutter renders real `yandex_maps_mapkit_lite` markers (imported under a prefix — its barrel collides with Flutter's own `Icon`/`TextStyle`/`ImageProvider`), coloured by role then freshness. The web panel loads the Yandex JS Maps API only if a **separate** `NEXT_PUBLIC_YANDEX_MAPS_JS_KEY` is configured (not yet — only the mobile MapKit key was supplied) — otherwise it falls back to a real, fully working list, never a blank/broken map. Still M4: the `pickup`/`delivery` map filters, which need Assignment/Delivery. | Owner §14-20: a real map now, built on what already exists; never fabricate what depends on unbuilt M3/M4 data. |
| D-038 | **The web panel gets a real test stack: `vitest` + React Testing Library**, mocking `fetch` at the network boundary rather than mocking `api.ts` itself (so the client's actual request/response handling, incl. 401 refresh and the `{error:{code,message}}` shape, is exercised for real). Chosen over Playwright/Cypress: component-level, no browser needed, fastest to wire into a `next build`-only CI so far, matches "не раздувай тестирование" (owner). Closes the previous round's largest disclosed gap (`MVP-ROADMAP.md` §5). | Owner §21: SUPER_ADMIN login/guard, users, roles, workers, manager scope, catalog, unauthorized redirect — kept to exactly that list. |
| D-039 | **Production Gate (owner, 2026-09-22): the first real NAS deployment, done over SSH with a dedicated key the owner set up specifically so this session never had to touch a password.** Found and fixed live (none of these showed up in stubbed/unit tests): `.env`'s unquoted `BACKUP_SCHEDULE_*` cron values broke `verify-stack.sh`'s `. ./.env` shell-sourcing; the same bug silently skipped the ADMIN bootstrap step on first install; `verify-stack.sh`'s persistent-volume check used host-side `ls` against a directory `chmod 700`'d for uid 999, misreporting "empty" as a permission error, not real emptiness; `prune()` in the shared backup library aborted the WHOLE backup job under `set -o pipefail` whenever a rotation tier had zero files yet (every non-Sunday `pg_dump` run, and every `config-backup.sh` run forever, since no age key is configured). All four fixed; backup+restore then verified live end-to-end (real `pg_dump`/MinIO mirror/config archive, full restore into a scratch database, byte-for-byte counts matched against production, scratch DB dropped). | Owner's Production Gate brief: prove M0-M2 on real infrastructure, not just tests; never hide a gap. |
| D-040 | **Colors get a real CRUD API** (owner, "Real Device → Colors/Variants → M3", 2026-09-22): `Color` already existed and was already referenced by `Material`/`ProductVariant`/`WorkAssignment` (M0 schema), but nothing could list/create one — the actual gap disclosed in the M2 round. Added `isActive`/timestamps (additive migration) and a `CATALOG_VIEW`/`CATALOG_MANAGE`-gated module mirroring Materials' shape exactly (list/create/update/deactivate, no hard delete). No PIM: name + optional hex only. | Owner §4: close the tech debt before Assignment, minimally — not a new product-information-management system. |
| D-041 | **M3 (WorkAssignment/Delivery/WorkProgress/QualityInspection/WorkerLedgerTransaction/CashPayment) is built on schema that already existed, unused, since M0** — same "the architecture already anticipated this" pattern as M2's materials/stock/kit, this time including the full `AssignmentStatus` lifecycle enum (DRAFT→…→COMPLETED, exact match to the owner's brief), every M3 realtime event (`assignment.*`, `work.*`, `delivery.*`, `quality.completed`, `earning.created`, `cash_payment.created`, `worker.balance_updated`), and `PayRateService.earning()` (its own comment: "used inside the acceptance transaction, M5" — M3 in the event). One `AssignmentsService.create()` transaction does all of §6 atomically (validate → deduct stock via the existing `StockService.recordMovement` → snapshot `WorkAssignmentMaterial` → issue an `ASSIGNMENT`-type QR (the `QrType` M2 already added for this) → create the `DELIVERY_TO_WORKER` delivery) — any failure rolls back the whole thing, verified by an actual insufficient-stock test. Earnings settle at acceptance time at the CURRENT global rate (D-027) and are never recalculated later. Cash payout refuses to exceed the ledger balance unless `forced:true` (audited). Found live by the M3 test suite (not by manual testing, which never created enough records fast enough to hit it): `catalog.service.ts`'s variant `sku` was built from `modelId`/`colorId` UUID *prefixes* — but those ids are UUIDv7 (time-ordered), so two variants created within the same broad time window collided on `sku @unique`; fixed to a proper `nextCode` sequence. | Owner §5-16: the whole work-order lifecycle, real money, real stock, real rollback — "не переделывай рабочую архитектуру" honoured by building on the M0 schema exactly as designed. |
| D-042 | **The pilot release APK (`1.0.0-rc.1`) surfaced two real, environment-specific build bugs no amount of `flutter analyze`/`flutter test` could have caught**, because neither performs an actual Android release build: (1) `yandex_maps_mapkit_lite` requires `minSdk 26`; the project defaulted to Flutter's own (lower) default — fixed by pinning `minSdk = 26` explicitly. (2) A release build has no cleartext-HTTP exemption (debug builds get one automatically from Flutter tooling) — added a `network_security_config.xml` allowing cleartext ONLY to the specific known NAS addresses (LAN IP, Tailscale IP, emulator host), never a blanket app-wide allowance; the real fix is the pending Cloudflare Tunnel + `diamoraa.uz` domain (HTTPS), this is the pilot-phase bridge until that's live. Also found: this dev machine's Gradle default (`-Xmx8G`) exceeds its actual 7 GB of RAM and reliably crashes the build daemon (OOM) under any concurrent load — lowered to `-Xmx3G`. A dedicated release keystore was generated for this pilot (git-ignored `key.properties`/`release-key.jks`, never committed) rather than shipping a debug-signed "release" build. | Owner §1: a real, installable release build, not a `flutter run --debug` — every one of these would have silently broken the very first real install. |

## Open owner actions

1. ~~Telegram bot token~~ — ✅ received 2026-09-21 (`@diamora1_bot`). It lives only in the NAS/dev `.env` (git-ignored). It was pasted into a chat, so **revoke and re-issue it in @BotFather (`/revoke`) before real workers use it** and put the new one in `.env`.
2. ~~Yandex MapKit key~~ — ✅ received 2026-09-21 (MapKit Mobile SDK; stored in the git-ignored `.env` as `YANDEX_MAPKIT_KEY`, passed to Flutter with `--dart-define`). Yandex activates keys ≈15 minutes after creation; it cannot be verified until the map screen exists (M4).
3. **Internet HTTPS:** a hostname for the Cloudflare Tunnel (or stay on the home network / Tailscale) — decision + steps in NAS-DEPLOYMENT.md §3.
4. ~~NAS model / architecture / RAM~~ — ✅ inspected over SSH (D-025). Change the NAS account password: it was shared in a chat.
5. ~~Rate per 9 m kit~~ — ✅ 30 000 UZS, editable in the ADMIN app (Профиль → Ставка за 9 м). The partial-kit rule is confirmed: pro rata (D-024).
6. **Who is SUPER_ADMIN on the real NAS deployment?** Not decided yet — the very first `docker compose run --rm api dist/cli/bootstrap.js --name "..." --phone "+998..."` on the NAS creates that account (D-032); pick the phone number before running it.
