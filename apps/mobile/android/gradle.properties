# -Xmx8G (the Flutter template default) exceeds this dev machine's total 7G RAM and crashes the Gradle daemon with an
# OOM native-allocation failure under any concurrent load (found building the pilot release APK, 2026-09-22). 3G leaves
# headroom for the OS and whatever else is running.
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
